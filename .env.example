# Kometa Preview Studio - Environment Configuration

# Backend server settings
PORT=3001
HOST=127.0.0.1
CORS_ORIGIN=http://localhost:5173

# Jobs and fonts directories
JOBS_PATH=./jobs
FONTS_PATH=./fonts

# Kometa Docker image tag (pinned for deterministic rendering)
# See available tags: https://hub.docker.com/r/kometateam/kometa/tags
KOMETA_IMAGE_TAG=v2.2.2

# Renderer image name (built from ./renderer)
KOMETA_RENDERER_IMAGE=kometa-preview-renderer:latest

# Optional: User asset directory (mount path on host)
# Uncomment and set to your Kometa asset directory for better artwork matching
# USER_ASSETS_PATH=/path/to/your/kometa/assets

# Optional: User Kometa config directory (for Original Posters backup)
# Uncomment to enable "Original Posters" backup fallback
# USER_KOMETA_CONFIG_PATH=/path/to/your/kometa/config
